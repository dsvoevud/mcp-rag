"""
test_indexer.py — Tests for document indexing and retrieval.

Covers:
    - Loading supported file types (.md, .txt, .py, .json, .yaml)
    - Chunking produces non-empty, correctly-sized chunks
    - ChromaDB upsert and retrieval round-trip
    - Index status returns correct document count
    - Unsupported file types are skipped gracefully
"""

import pytest

# TODO: implement tests


def test_placeholder():
    """Placeholder — replace with real tests."""
    assert True
